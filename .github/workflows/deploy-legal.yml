# Publishes legal/ → GitHub Pages (Actions deploy).
# Live URLs (repo: hansaglam/identidadeapp):
#   https://hansaglam.github.io/identidadeapp/privacy.html
#   https://hansaglam.github.io/identidadeapp/terms.html
#
# One-time: Settings → Pages → Source: GitHub Actions

name: Deploy legal pages

on:
  push:
    branches: [main, master]
    paths:
      - "legal/**"
      - ".github/workflows/deploy-legal.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: deploy-legal-pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./legal

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
